using MonoForge.Rendering.Batching;

namespace MonoForge.UI;

public class Canvas : IObject, IDrawable, IUpdatable
{
    public void Draw(GameBase gameBase, IRenderQueue renderQueue)
    {
    }

    public void Update(GameBase gameBase, float deltaTime)
    {
    }

    public void Dispose()
    {
    }
}